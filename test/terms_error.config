{erl_opts, [
    debug_info,
    fail_on_warning
]}.

{xref_checks, [
    undefined_function_calls
]}  % missing period

{cover_enabled, true}.
{cover_print_enabled, true}.

io:format("hello\n").  % bad term
